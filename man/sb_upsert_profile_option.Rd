% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_option.R
\name{sb_upsert_profile_option}
\alias{sb_upsert_profile_option}
\title{Set option parameters for \code{\link[=sb_upsert_profile_option]{sb_upsert_profile_option()}}}
\usage{
sb_upsert_profile_option(
  interactive_mode = TRUE,
  id_col = c("user_id", "about", "username", "email"),
  table_field = NULL,
  cache = TRUE
)
}
\arguments{
\item{interactive_mode}{If TRUE, all messages are printed to the console.
If FALSE, they are suppressed. The idea is that \code{interactive_mode} should be
set to FALSE to ensure logs aren't clogged up with progress messages in
automated environments.}

\item{id_col}{Name of column that \code{smartabaseR} should use
for user identifier. By default the Smartabase API requires a "user_id"
column, but other possible values are "about", "username" or "email".}

\item{table_field}{Smartabase tables are used when you want to store
multiple rows of data in one event. If you are importing data into a form
that contains table fields, you must manually supply the names of those
fields as a character vector to the \code{table_field} argument within the
relevant import option helper function. This is equivalent to ticking
'Treat all records for the same user, on the same day as a single record?'
in the Smartabase web app.}

\item{cache}{If TRUE, login token and user details are cached once per
session. To invalidate the cache, restart your R session.}
}
\value{
A list of options with class = "sb_import_option"
}
\description{
Set option parameters for \code{\link[=sb_upsert_profile_option]{sb_upsert_profile_option()}}
}
\examples{
\dontrun{
# Calling the function with no arguments returns all the defaults
sb_upsert_profile_option()

# Specifying specific arguments will alter those arguments alone while still
# returning the other defaults
sb_upsert_profile_option(
  id_col = "about",
  table_field = c("Example Table Field Name")
)
}
}
\seealso{
\code{\link[=sb_insert_event_option]{sb_insert_event_option()}}
\code{\link[=sb_update_event_option]{sb_update_event_option()}}
\code{\link[=sb_upsert_event_option]{sb_upsert_event_option()}}

Other import option helpers: 
\code{\link{sb_insert_event_option}()},
\code{\link{sb_update_event_option}()},
\code{\link{sb_upsert_event_option}()}
}
\concept{import option helpers}
